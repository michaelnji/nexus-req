const config = {
    entries: [
        {
            filePath: "./src/index.ts",
            outFile: "./dist/nexus-req.d.ts",
            noCheck: false,
        },
    ],
};

module.exports = config;
