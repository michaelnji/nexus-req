const config = {
    entries: [
        {
            filePath: "./src/index.ts",
            outFile: "./dist/response-rat.d.ts",
            noCheck: false,
        },
    ],
};

module.exports = config;
